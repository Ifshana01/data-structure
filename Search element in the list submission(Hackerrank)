#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N, target;
    scanf("%d", &N);
    int arr[N];
    for(int i=0;i<N;i++){
        scanf("%d", &arr[i]);
    }
    int flag =0;
    scanf("%d", &target);
    for(int i=0;i<N;i++){
        if(arr[i]==target){
            flag =1;
        }
    }
    if(flag==0){
        printf("Not Found");
    }
    else{
        printf("Found");
    }
    
    return 0;
}
